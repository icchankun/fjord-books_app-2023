<p style="color: green"><%= notice %></p>

<h1><%= Book.model_name.human(count: 2) %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to t('action.show'), book %>
    </p>
  <% end %>
</div>

<%= link_to t('action.new'), new_book_path %>
