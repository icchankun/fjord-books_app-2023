<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('action.edit'), edit_book_path(@book) %> |
  <%= link_to t('action.index'), books_path %>

  <%= button_to t('action.destroy'), @book, method: :delete %>
</div>
